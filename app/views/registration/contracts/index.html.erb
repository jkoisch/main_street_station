<h1>Listing registration_contracts</h1>

<table>
  <tr>
    <th>Community</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @registration_contracts.each do |registration_contract| %>
  <tr>
    <td><%= registration_contract.community_id %></td>
    <td><%= registration_contract.status %></td>
    <td><%= link_to 'Show', registration_contract %></td>
    <td><%= link_to 'Edit', edit_registration_contract_path(registration_contract) %></td>
    <td><%= link_to 'Destroy', registration_contract, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Contract', new_registration_contract_path %>
