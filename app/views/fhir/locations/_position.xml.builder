xml.longitude({value: position.longitude}) unless position.longitude.nil?
xml.latitude({value: position.latitude}) unless position.latitude.nil?
xml.altitude({value: position.altitude}) unless position.altitude.nil?
