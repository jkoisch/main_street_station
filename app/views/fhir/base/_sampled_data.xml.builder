xml.origin({value: sampled_data.origin}) if sampled_data.origin
xml.period({value: sampled_data.period}) if sampled_data.period
xml.factor({value: sampled_data.factor}) if sampled_data.factor
xml.lower_limit({value: sampled_data.lower_limit}) if sampled_data.lower_limit
xml.upper_limit({value: sampled_data.upper_limit}) if sampled_data.upper_limit
xml.dimensions({value: sampled_data.dimension}) if sampled_data.dimensions
xml.data({value: sampled_data.data}) if sampled_data.data