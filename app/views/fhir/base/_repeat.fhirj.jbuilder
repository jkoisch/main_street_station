json.frequency  repeat.frequency unless repeat.frequency.nil?
json.when       repeat.when unless repeat.when.nil?
json.duration   repeat.duration unless repeat.duration.nil?
json.units      repeat.units unless repeat.units.nil?
json.count      repeat.count unless repeat.count.nil?
json.end        repeat.end unless repeat.end.nil?